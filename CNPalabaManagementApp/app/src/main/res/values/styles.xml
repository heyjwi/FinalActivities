<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <!-- Set the font family -->
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
    </style>

    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <!-- Set the font family -->
        <item name="android:fontFamily">@font/inter</item>
        <!-- Set the text size -->
        <item name="android:textSize">20dp</item>
        <!-- Set the font weight -->
        <item name="android:textStyle">normal</item>
        <!-- Set the text color -->
        <item name="android:textColor">@color/dark_color</item>
        <item name="android:textAllCaps">false</item>

    </style>

    <style name="colored_checkBox" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorAccent">@color/light_red</item>
        <item name="android:textColorSecondary">@color/light_red</item>

    </style>

    <style name="PillSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/spinner_bg</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:popupBackground">@android:color/white</item>
    </style>

</resources>