<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state color -->
    <item android:color="@color/light_red" android:state_checked="true"/>
    <!-- Unchecked state color -->
    <item android:color="@color/dark_color" android:state_checked="false"/>
    <!-- Default color -->
    <item android:color="@color/dark_color"/>
</selector>